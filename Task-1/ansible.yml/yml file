---
- name: java installation
  become: yes
  hosts: all
  vars: java_package_name=openjdk-11-jdk
  tasks:
    - name: java install
      ansible.builtin.package:
        name: "{{ java_package_name }}"
        state: present